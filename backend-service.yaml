apiVersion: v1
kind: Service
metadata:
  name: nestjs-oracle-backend-service
  labels:
    app: nestjs-oracle-backend
spec:
  type: LoadBalancer
  selector:
    app: nestjs-oracle-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
